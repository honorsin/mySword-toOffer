var solveNQueens = function (n) {
    const result = [];
    const recrusion = (path, set) => {
        if (path.length === n) {
            result.push(path);
        }
        for (let i = 0; i < n; i++) {

        }
    }
};